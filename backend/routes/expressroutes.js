const express = require('express');
const router = express.Router();

// Import your appointment model
const Appointment = require('../models/Appointment');

// Routes to handle API requests will go here

module.exports = router;
